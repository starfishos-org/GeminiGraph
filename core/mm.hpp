// #pragma once
// #include <numa.h>

// // #define USE_NUMA_ALLOC
// #define USE_NORM_ALLOC

// inline void *gemini_alloc(size_t size) {
// #ifdef USE_NUMA_ALLOC
//   return 
//   void * ptr = numa_alloc(size);
//   assert(ptr!=NULL);
//   return ptr;
// }